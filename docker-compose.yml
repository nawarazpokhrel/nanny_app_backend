version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8002
    volumes:
      - .:/home/app/webapp
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True

volumes:
  postgres_data:
